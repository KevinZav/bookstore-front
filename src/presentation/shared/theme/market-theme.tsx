import { createTheme } from "@mui/material";

export const marketTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#1E3A8A'
    },
    secondary: {
      main: '#B91C1C'
    }
  }
});