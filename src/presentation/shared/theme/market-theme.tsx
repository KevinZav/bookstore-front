import { createTheme } from "@mui/material";

export const marketTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      "100": '#0000ff',
      main: '#FDBA74'
    },
    secondary: {
      main: '#60A5FA'
    }
  }
});